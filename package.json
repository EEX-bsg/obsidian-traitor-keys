{
  "name": "obsidian-traitor-keys",
  "version": "0.1.0",
  "description": "Obsidian plugin that injects Emacs-style Ctrl navigation keys into Vim insert/visual modes.",
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "generate": "rm -rf dist && mkdir -p dist && yarn build --production && cp manifest.json dist/ && cp versions.json dist/ && (test -f styles.css && cp styles.css dist/ || true)",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@codemirror/commands": "^6.6.0",
    "@codemirror/state": "^6.4.0",
    "@codemirror/view": "^6.30.0",
    "@types/node": "^20.11.0",
    "esbuild": "^0.23.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.0"
  }
}
